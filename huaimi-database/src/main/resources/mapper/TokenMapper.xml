<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ieng.huaimi.database.mapper.TokenDao">

  <resultMap id="BaseResultMap" type="com.ieng.huaimi.database.domain.Token">
    <id column="user_id" property="userId" />
    <result column="token" property="token" />
    <result column="expire_time" property="expireTime" />
    <result column="update_time" property="updateTime" />
  </resultMap>

  <sql id="Base_Column_List">
    user_id, token, expire_time, update_time
  </sql>

  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from t_token
    where user_id = #{userId}
  </select>

  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
    delete from t_token
    where user_id = #{userId}
  </delete>

  <insert id="insert" parameterType="com.ieng.huaimi.database.domain.Token">
    insert into t_token (user_id, token, expire_time, update_time)
    values (#{userId}, #{token}, #{expireTime}, #{updateTime})
  </insert>

  <insert id="insertSelective" parameterType="com.ieng.huaimi.database.domain.Token">
    insert into t_token
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="userId != null">
        user_id,
      </if>
      <if test="token != null">
        token,
      </if>
      <if test="expireTime != null">
        expire_time,
      </if>
      <if test="updateTime != null">
        update_time,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="userId != null">
        #{userId},
      </if>
      <if test="token != null">
        #{token},
      </if>
      <if test="expireTime != null">
        #{expireTime},
      </if>
      <if test="updateTime != null">
        #{updateTime},
      </if>
    </trim>
  </insert>

  <update id="updateByPrimaryKeySelective" parameterType="com.ieng.huaimi.database.domain.Token">
    update t_token
    <set>
      <if test="token != null">
        token = #{token},
      </if>
      <if test="expireTime != null">
        expire_time = #{expireTime},
      </if>
      <if test="updateTime != null">
        update_time = #{updateTime},
      </if>
    </set>
    where user_id = #{userId}
  </update>

  <update id="updateByPrimaryKey" parameterType="com.ieng.huaimi.database.domain.Token">
    update t_token
    set token = #{token},
      expire_time = #{expireTime},
      update_time = #{updateTime}
    where user_id = #{userId}
  </update>

</mapper>